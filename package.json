{
  "name": "agent-runtime",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "turbo build",
    "db:migrate": "pnpm --filter @agent-runtime/api db:migrate",
    "dev": "turbo dev",
    "typecheck": "turbo typecheck"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  },
  "devDependencies": {
    "turbo": "latest",
    "typescript": "latest"
  }
}
